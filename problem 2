#include<iostream>
#include<Windows.h>
using namespace std;

int main() {

	int freq;
	int dur;
	int n;
	//gets and store keyboard info from user:
	cout << "please enter three numbers (frequency, duration, number):" << endl;
	cin >> freq;
	cin >> dur;
	cin >> n;
	//this plays a beep of pitch freq and duration dur n times 
	for (int i = 0; i < n; i++)
		Beep(freq, dur);
}
